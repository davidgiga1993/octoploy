name: 'var-loader-app'

vars:
  CERT:
    loader: pem
    file: dummy.pem
  EXT_YML:
    loader: file
    conversion: yml
    file: test.yaml.txt

configmaps:
  - name: nginx-config
    files:
      - file: "sample.txt"