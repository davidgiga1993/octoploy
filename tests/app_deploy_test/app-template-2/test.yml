kind: DeploymentConfig
apiVersion: v1
metadata:
  name: "${PARAM_A}"
  name2: "${SECOND}"
  base: "${BASE}"
  APP_NAME: "${APP_NAME}"
  GLOBAL_TEST: ${globalVar}
  REMAPPED: ${REMAPPED}
  REMAPPED2: ${REMAPPED2}