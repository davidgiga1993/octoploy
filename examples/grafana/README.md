This example contains a standalone grafana definition.
You'll have to modify the `DOMAIN` variable inside `_index.yml`.

You can deploy grafana using `python deploy.py --config-dir examples deploy grafana`